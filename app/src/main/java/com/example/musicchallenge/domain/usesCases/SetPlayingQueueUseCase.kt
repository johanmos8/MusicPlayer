package com.example.musicchallenge.domain.usesCases


import com.example.musicchallenge.domain.models.Song
import com.example.musicchallenge.domain.repositories.IMusicRepository
import javax.inject.Inject

/*class SetPlayingQueueUseCase @Inject constructor(private val songRepository: IMusicRepository) {
    suspend operator fun invoke(songs: List<Song>) = songRepository.setPlayingQueue(songs)
}*/
